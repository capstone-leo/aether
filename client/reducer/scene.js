const initialState = {};

const LOAD_SCENE = 'LOAD_SCENE';

const loadScene = (scene) => ({
  type: LOAD_SCENE,
  scene,
});

//thunk
//const startScene =

export default (state = initialState, action) => {};
